package com.unimib.ignitionfinance.domain.usecase.networth

import com.unimib.ignitionfinance.domain.usecase.GetProductListUseCase

class GetUserInvestedUseCase {
    //in input una product list e ritornare la somma di amount per ogni singolo prodotto
    //private val productList: GetProductListUseCase.execute

    /*foreach(rodcutlist)
    **/
}